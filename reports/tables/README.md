# Tables

Таблицы метрик и результатов экспериментов сохраняются в подкаталоги по
эксперименту. Каталог `baseline/` содержит уже существовавшие результаты
`TF-IDF + Logistic Regression`; ноутбук DistilBERT создаёт таблицы в
`distilbert_full_ft/` при запуске обучения и оценки.
